# Prefabs

This folder will hold the master Prefabs built per `docs/07_Setup_Instructions.md` §5.1. They are created in-Editor (cannot be committed pre-asset-import because they depend on imported Asset Store meshes).
